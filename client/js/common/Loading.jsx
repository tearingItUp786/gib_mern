import React from 'react';
import { SpinnerContainer, DonutSpinner } from '../styling/Loading';

const Loading = () => (
  <SpinnerContainer>
    <DonutSpinner />
  </SpinnerContainer>
);

export default Loading;
